<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
  <head>
    <title><%= yield(:title) || "SugarSave" %></title>
    <link rel="shortcut icon" href="/favicon.ico" type="image/ico" />
    <%= stylesheet_link_tag 'reset', 'welcome', 'flash', 'facebox' %>
    <%= javascript_include_tag 'jquery', 'welcome', 'facebox' %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <%= render :partial => '/layouts/welcomeheader' %>
    
    <div id="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}", :class => "hideable" %>
      <% end %>
      
      <div id="content">
        
        <%= yield %>

      </div>
    </div>
    
    <%= render :partial => '/layouts/footer' %>
    
    <%= render :partial => '/shared/picker' %>
  </body>
</html>
